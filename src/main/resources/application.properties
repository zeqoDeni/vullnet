spring.application.name=Vullnet0.0
spring.datasource.url=jdbc:mysql://localhost:3306/vullnet_db
spring.datasource.username=vullnet_user
spring.datasource.password=Vullnet#2026!Pass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.cors.allowed-origins=http://localhost:8000,http://localhost:3000

app.jwt.secret=${APP_JWT_SECRET:change-me-in-prod-change-me-in-prod}
app.jwt.expiration-minutes=1440

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

logging.pattern.level=%5p [req:%X{requestId}]

app.limits.max-open-requests=10

app.admin.email=
app.admin.password=
app.admin.first-name=Admin
app.admin.last-name=User
